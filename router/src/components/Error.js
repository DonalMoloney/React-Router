import React from "react";
//Component is displayed when a user goes to page that does not exist
const Error = () =>{
  return (
    <div>
      <p>The page you are trying to find does not exist</p>
    </div>
  );
};

export default Error
