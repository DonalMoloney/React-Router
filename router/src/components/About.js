import React from "react";
// What appears when user types in localhost:300/about in the browser

const About = () =>{
  return (
    <div>
      <p>About</p>
    </div>
  );
};

export default About
