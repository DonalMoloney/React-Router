import React from "react";
// What appears when user types in localhost:300/contact in the browser
const Contact = () =>{
  return (
    <div>
      <p>Contact</p>
    </div>
  );
};

export default Contact
